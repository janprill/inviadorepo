<%= form_with(model: tagging, local: true) do |form| %>
  <% if tagging.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tagging.errors.count, "error") %> prohibited this tagging from being saved:</h2>

      <ul>
        <% tagging.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :taggable_id %>
    <%= form.text_field :taggable_id %>
  </div>

  <div class="field">
    <%= form.label :taggable_type %>
    <%= form.text_field :taggable_type %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :raw %>
    <%= form.text_field :raw %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
