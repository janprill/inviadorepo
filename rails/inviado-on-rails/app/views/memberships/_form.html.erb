<%= form_with(model: membership, local: true) do |form| %>
  <% if membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(membership.errors.count, "error") %> prohibited this membership from being saved:</h2>

      <ul>
        <% membership.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :raw %>
    <%= form.text_field :raw %>
  </div>

  <div class="field">
    <%= form.label :membershipable_id %>
    <%= form.text_field :membershipable_id %>
  </div>

  <div class="field">
    <%= form.label :membershipable_type %>
    <%= form.text_field :membershipable_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
